# Default configuration
# This file contains shared configurations

# Variables for GeoIP
map $remote_addr $geoip_country_code {
    default "";
}

map $remote_addr $geoip_country_name {
    default "";
}

# Rate limit zone
limit_req_zone $binary_remote_addr zone=global_rate_limit:10m rate=100r/s;

# Connection limit zone
limit_conn_zone $binary_remote_addr zone=global_conn_limit:10m;
